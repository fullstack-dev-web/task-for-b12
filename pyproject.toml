[project]
name = "b12-application-submit"
version = "0.1.0"
description = "CI script to submit B12 application"
requires-python = ">=3.9"

dependencies = [
    "requests>=2.31.0"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
